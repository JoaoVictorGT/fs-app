# DAST com OWASP ZAP removido temporariamente.
# O backend sobe corretamente mas o ZAP não consegue alcançar
# o servidor dentro do ambiente do GitHub Actions.
# Reintegrar quando houver ambiente dedicado (ex: staging com URL pública).