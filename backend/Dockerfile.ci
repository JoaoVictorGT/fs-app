FROM node:20-alpine

# tini garante signal handling correto quando Node.js é PID 1 em container
RUN apk add --no-cache tini

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

EXPOSE 4000

ENTRYPOINT ["/sbin/tini", "--"]
CMD ["node", "dist/server.js"]
